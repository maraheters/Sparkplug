spring:
  datasource:
    url: jdbc:tc:postgresql:16.6:///test_database
    username: postgres
    password: password
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration/test
    schemas: catalog